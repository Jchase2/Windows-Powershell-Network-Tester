# Windows-Powershell-Network-Tester
A PowerShell script modeled on the netAlly LinkSprinter, built to automate network diagnostic commands. Runs connectivity checks, DNS lookups, and reports results in a readable format.


